import React, { createContext } from "react";

let CricketContext = createContext();

export default CricketContext;
/* 
CricketContext = {
    Provider: component => add values
    Consumer: component => access value
}

<CricketContext.Provider/>
*/
